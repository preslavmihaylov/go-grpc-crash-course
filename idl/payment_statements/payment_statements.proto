syntax = "proto3";

package payment_statements;
option go_package = "gitlab.com/preslavmihaylov/go-grpc-exercise/gen/payment_statements";

import "common/types.proto";

service PaymentStatements {
  rpc CreateStatement(stream types.Payment) returns (types.PaymentStatement);
}
